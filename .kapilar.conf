# MultiSych — kapı listesi.
# Biçim: ad|komut|beklenen-desen   (desen boşsa yalnız çıkış kodu 0 aranır)
#
# Desen ZORUNLU tutuldu: bir kapının "başarıyla hiçbir şey yapmaması"
# yeterli sayılmaz — çıkış kodu değil, SÖYLEDİĞİ ŞEY doğrulanır.
# (bkz. ~/.claude/CLAUDE.md §3, §4 ve docs/KURALLAR.md §2)
#
# Koşum: `tools/kapilar` (proje kökünden). ~180sn/kapı zaman aşımı.

dotnet-sdk|bash scripts/check-dotnet-sdk.sh|kapı yeşil
derleme|dotnet build MultiSych.slnx|Build succeeded
test|dotnet test MultiSych.Tests/MultiSych.Tests.csproj|Passed!
guvenlik-baslangic|bash tools/guvenlik-baslangic-testi.sh|kapı yeşil
bildirim|bash tools/bildirim-testi.sh|kapı yeşil
erisim|bash tools/erisim-testi.sh|kapı yeşil
eksiklik|bash tools/eksiklik-testi.sh|kapı yeşil
lokalizasyon|bash tools/lokalizasyon-testi.sh|kapı yeşil
